# WoW Forever 1.60.1.69913

Running compatibility report for the authenticated `wow_classic_beta` build with interface `16001`. This records committed work and measured evidence only; pending work is updated incrementally rather than inferred from source presence.

## Scope

- Profile: `client-wowforever`
- Source branch: `forever`
- Runtime cache: `~/.cache/wow-ui-sim/blizzard-ui/wowforever/AddOns`
- Product: `wow_classic_beta`
- Build: `1.60.1.69913`
- Interface: `16001`

## Pinned external evidence

Warcraft Wiki's Forever patch page links to Ketho's BlizzardInterfaceResources dump. Its pinned [`forever` revision `659e8042049df854c114714f8ecd640823a1cd5c`](https://github.com/Ketho/BlizzardInterfaceResources/tree/659e8042049df854c114714f8ecd640823a1cd5c) reports `GetBuildInfo()` as `1.60.1.69913`, interface `16001`. Its immutable [Lua enum dump](https://raw.githubusercontent.com/Ketho/BlizzardInterfaceResources/659e8042049df854c114714f8ecd640823a1cd5c/Resources/LuaEnum.lua) sets `LE_LFG_CATEGORY_LAIR = 8`. Its CVar dump says possess `"1"` and stance `"3"`, but a user-supplied screenshot from that build shows possess current/default `4`/`4`, stance `3`/`3`, and `C_EditMode.GetEditModeDefaultLayout() = 0`; `31a7e37f3` treats the direct observed possess value as controlling. Layout `0` is an observation, not a general selection rule. Direct Warcraft Wiki/wowdev pages did not supply these Forever values; mutable mainline/PTR tables are not used as evidence.

## Keyring inventory query

`5a73449b8` adds Forever-only `HasKey()` backed by occupied keyring inventory slots. Targeted tests passed **2/2**, covering insertion/removal, zero stacks, ordinary-bag exclusion and the real acknowledged-tutorial consumer. The missing-global RED was reproduced; the first consumer RED instead exposed a missing fixture prerequisite. Other-profile/final checks remain pending. See [the contract and inventory policy](specs/forever-has-key.md).

## Committed changes

| Area | Commits | Committed behavior / current proof |
|---|---|---|
| Distinct profile | `abbc1272f` | Adds `WowForever`; it is not an Era or Anniversary alias. Selects the `wowforever` cache, `_classic_beta_` install path, Camelot game directory, and interface `16001`. |
| Source inventory | `f2619520c`, `d1ebc389e` | Adds the 4,398-file Forever manifest and maps `wowforever` to the Gethe `forever` source branch. |
| CASC mappings and cache sync | `65139f909` | Refreshing the community listfile added 592 generated path-to-FDID rows. Rebuilding completed sync of all 4,398 manifest files; the verifier recorded one existing CDN recovery, so this is not an all-local-CASC claim. |
| Loader-bound `require` | `be073174d`, `55c9b3d27`, `964358618`, `78cf08372`, `23928cfb7` | Forever-only `require` resolves completed addon Lua modules, retains values/provenance across GC, enforces direct TOC dependencies for disk callers, and never becomes filesystem or `package` loading. |
| Family TOC routing | `5e26960b6`, `5820b54e6`, `f52b57a15` | Corrects `[Family]` from `Classic` to `Mainline`, then corrects accepted inline tags to `camelot`/`mainline`; `[Game]` remains `Camelot`. `f52b57a15` establishes deterministic Camelot → generic → Mainline TOC selection, allowing the Mainline-only `Blizzard_SharedMapDataProviders` RequiredDep to enter discovery; it removes the prior WorldMap-specific exception. RED (`5/7`) then GREEN (`7/7`) covers profile fixtures, dependency order, and provider fixture execution. Full vendor startup remains unrun. |
| Math utilities | `0a354c0e0` | Reuses the simulator's existing math extensions for Forever so `MathUtil.lua` can publish `Round` and related aliases. A real `MathUtil.lua` fixture covers the aliases and extension behavior; no retail API epoch is enabled. |
| Finite event registration | `4deca63f1`, `725abfb8b`, `25c6ac778` | Adds finite source-documented Forever acceptance for additional startup events. `725abfb8b` adds ten generated-documentation `LiteralName` values, and `25c6ac778` adds later Discord/social/ping/pet-training names; invented names remain rejected. This is registration acceptance, not producer-state modeling. |
| Timed signal maps | `799389a6e` | Extracts a `timed-signal-maps` capability shared by PTR 12.1.5 and Forever. It exposes the existing `C_Timer.NewTimedSignalMap` state, scheduling, and `TimerUtil.lua` consumer without enabling a retail epoch. |
| Table utilities | `b6a6a7dad` | Reuses existing table extensions for Forever so `TableUtil.lua` can publish its compatibility aliases. The real-source fixture is committed; final verification remains pending. |
| `securecopy` | `0d7942e64` | Moves the existing cycle-safe deep-copy compatibility helper from PTR bootstrap scope into a shared temporary workaround for PTR and Forever. Its tests cover nested/cyclic table independence and userdata identity; no metatable or taint-copy claim. |
| Texture atlases | `5cd134dcd` | Generates and selects exact `1.60.1.69913` DB2/listfile atlas data with provenance: 19,203 entries and 20,684 elements. It records 2,306 skips (1,560 missing-path rows, 55 FDIDs, 746 duplicates), avoiding fabricated rectangles; no full texture-coverage claim. |
| Source-documented enums and gamepad constants | `5e2558264`, `b838d6917`, `7f6297984` | Publishes Forever `BattleNetFriendLevel`, `VisualAlertType`, and `CooldownViewerSound` with metadata, plus `Constants.GamepadActionBarConstants`. Vendor-consumer regressions cover enum values and real action-button initialization; this does not implement interactive gamepad input. |
| Input interface style | `a0e6df424` | Adds Forever `InputDeviceInterfaceType`, `C_InputInterfaceStyle.GetCurrentStyle()`, finite transition-event handling, and callback dispatch state. Real `InputUtil.lua` initialization/transition fixtures pass; no host-gamepad delivery claim. |
| Social enums | `916acf5e8`, `27e57ec4e` | Publishes source-backed social enum values/metadata, including the remaining presence/system groups. Focused documentation and consumer proof is GREEN (`3/3`); unavailable social-service behavior is not stubbed. |
| Edit Mode enums | `5955650e8` | Publishes finite source-backed preset/system enums and metadata through a shared C API registration path. Development proof is GREEN (`3/3`) for actual Mainline presets, Shared setting display, and source equality across 15 namespaces; no retail epoch is enabled. |
| Cooldown categories | `413fcd1ce` | Publishes documented Forever cooldown-viewer category values 0–8 and metadata. The actual settings data provider is GREEN (`1/1`); this does not establish cooldown population or interactive settings behavior. |
| Regional name policy | `6f9877555`, `df74659ea` | Models Forever regional unique-name availability through existing player-identity state with an explicit simulator default of `false`. Actual Camelot `NameUtil` proof is GREEN (`2/2`); default-profile exclusion remains pending. |
| Texture metatable | `7da2b6028`, `343c4624b`, `f4800730c` | Exposes the real texture metatable helper required by `UnitFrameUtil.lua`. The actual consumer went RED then GREEN (`1/1`) for `__index` identity, type, and mutation; default-profile exclusion remains pending. |
| Roleset methods | `e3dfe88a3`, `1532a3cc8` | Shares existing `AddRoleset`, `GetRolesetNames`, `RemoveRoleset`, and `SetRolesets` with Forever while leaving the retail-only security method group unchanged. RED (`0/1`) then warning-free GREEN (`1/1`) covers membership, replacement, removal, clear, isolation, and metatable publication. |
| Rilua table security | rilua `122cfc9`–`7cdef27`, simulator `29f95e6f5`, `5a671fce9`, `5ebe21a46`, `4f92e7cb0`, `60ae94004` | Forever registers rilua's native table-security globals after compatibility initialization and before secure-environment copying; other profiles remain unchanged. `5ebe21a46` pins published Osso/rilua `main` at `7cdef27ca7f856a3b30608f41918980e79fb346c`, which consumes closure stamps at call entry, preserves them through tail-call reuse, and lets secure calls clear caller taint without erasing callee stamps. Table flags enforce documented `DisallowTaintedAccess` and `DisallowSecretKeys` through VM, raw, iterator, metamethod, and metatable paths. Core (`8/8`), VM (`3/3`), stdlib (`4/4`), closure (`2/2`), and table-security (`15/15`) development suites passed. Forever integration is GREEN (`4/4`): authenticated `CooldownViewerSecure.lua` maps preserve plain/wrapped/copied key identity, reject secret backing-table and explicit-caller taint access, reject real addon-loader stamped closures, and retain native globals after cleanup. `SecretWrapContents` fails explicitly; opaque GC-traced secret keys support this Cooldown Viewer boundary only, not a full secret-value VM or native-conformance claim. |
| Inventory slot methods | `68989ffcf`, `25442752c` | Publishes existing `C_PaperDollInfo` inventory-slot methods and loads required equipment-flyout data. Real Head/MainHand/Ammo button behavior is GREEN (`2/2`); the isolated vendor RED also exposed fixture prerequisites. |
| Finite UI constants | `db875f554`, `97ef67031` | Publishes source-backed Minimap/Ping/Transmog/Gamepad possess-bar values. Actual Minimap, Ping, and Transmog consumer loading is GREEN (`3/3`); Transmog's remaining startup root was not attributed to `NoTransmogID` alone. |
| Build-specific global strings | `f954fc665` | Adds 27,262 build-specific `GlobalStrings.csv` tags with provenance and a generator. GameplaySettingsGroup/string restoration is GREEN (`2/2`); generator fixtures are GREEN (`2/2`). |
| LFG lair category | `3c800b221` | Publishes exact-build `LE_LFG_CATEGORY_LAIR = 8` and category count `8`. RED (`0/1`) then GREEN (`1/1`) loads cached `Constants.lua`, validates LFG mappings, and reaches `MAX_WORLD_PVP_QUEUES = 2`. See `/tmp/forever-lfg-ledger.json`. |
| Aura XML widgets | `c20f26dad` | Shares existing AuraContainer, ManagedAuraContainer, and AuraButton XML/factory support with Forever. Nested schema and widget-behavior fixtures are GREEN (`2/2`); this is not a full vendor UnitFrame load claim. |
| Gamepad stick scripts | `72fc53d24`, `5570fe263`, `d5ad14601` | Adds canonical `OnGamePadStick` and the Forever Lua `OnGamepadStick` alias across script APIs, with generic `(stick, x, y)` dispatch; follow-ups load those handlers from XML/templates and correct template-handler array length. It does not enable host-gamepad delivery or establish native alias validation. |
| Gamepad override events and CVar defaults | `10826d74c`, `36f9ee9e8`, `22064dca1`, `31a7e37f3` | Registers documented Forever override events and state-backed character CVar defaults. The public dump's possess `"1"` conflicts with the user-supplied exact-build screenshot's current/default `4`/`4`; `31a7e37f3` corrects possess to `"4"` while retaining stance `"3"`. RED (`3/5`) then GREEN (`5/5`) covers the observed defaults, Page1BottomBar initial positioning, explicit `"1"` SpecialPageTopBar positioning, writes, and events. The first replay proved inherited possess/stance `OnLoad` handlers aborted at `RegisterEvent`, not missing inherited scripts or parent/child ordering. Independent verification accepts this bounded correction: formatting/default/Forever checks pass without warnings, and the five behavioral results were inspected. See `/tmp/forever-native-cvar-ledger.json` and `/tmp/verify-forever-native-cvar/ledger.json`; no post-correction startup count is claimed. |
| Gamepad stance enum | `cbccbf6ad` | Publishes exact-build stance override enum and metadata. RED (`0/1`) then grouped GREEN (`6/6`) includes unchanged vendor stance initialization, twelve mappings, default anchor, and `None` behavior. See `/tmp/forever-stance-ledger.json`. |
| Gamepad pet storage | `2d2877b25`, `91a73d59d` | Adds a named simulator policy returning the existing logical pet-slot base `1` to `C_GamepadUI.GetFirstGamepadPetActionStorageSlotIndex()`. Native numeric equivalence remains unverified; the policy is coherent with existing one-based pet APIs, not a claimed client offset. RED (`0/1`) then GREEN (`6/6`) covers eight vendor pet-button IDs, populated slots, cooldowns, removal, and unchanged ordinary action-bar slot `1`. See `/tmp/forever-pet-storage-ledger.json`. |
| EditMode default layout | `3d682af7b` | Models Forever's named initial default as Modern (`0`), matching the supplied observed output without generalizing a native input-selection policy. RED (`0/1`) then GREEN (`4/4`) covers the actual Camelot preset-manager anchor lookup and copied state. See `/tmp/forever-default-layout-ledger.json`. |
| Unit interaction state | `77dbda577`, `bf3f12447`, `9dcb0a78a`, `58ac14095` | Models resolved-unit game-object, loot, interactability, and range state plus the `softinteract` alias. GREEN (`3/3`) covers direct/soft interaction and actual vendor icon-condition branches. Fixture preferred-target/cursor/context behavior is intentionally scoped; it is not proof of `SetPreferredGamepadInteractTarget`. See `/tmp/forever-unit-interaction-ledger.json`. |
| Preferred Gamepad interaction target | `7e449f911` | Adds Forever-only state-backed `SetPreferredGamepadInteractTarget`, storing resolved GUID identity and clearing on nil or unknown input. RED (`0/2`) then GREEN (`2/2`) covers actual `GamepadMainActionBarFrameMixin:UpdateInteractIcons()` target selection, core-binding clear, alias identity, and no-retarget behavior. No getter, event, host-input, or native invalid-token behavior is inferred. See `/tmp/forever-preferred-ledger.json`. |
| Weapon-enchant snapshots | `bc6fbbe3e`, `e0e753442`, `ad2785975` | Models `C_Item.GetWeaponEnchantInfo()` from shared legacy weapon-enchant state so it returns slot snapshots rather than `nil`. The original GREEN (`3/3`) was incomplete: verification found exact-build `WeaponSlot.Ranged = 2`, while the model rejected that slot. `ad2785975` expands state and the Forever enum to all three slots; legacy `GetWeaponEnchantInfo()` remains the documented eight MainHand/OffHand returns. RED then GREEN (`4/4`) covers ranged snapshots, invalid slot 3, legacy compatibility, and real `BuffFrame` handling of all three IDs. See `/tmp/forever-ranged-ledger.json`. |
| Raid-marker constants | `027597911`, `ec3491356` | Publishes documented `Constants.RaidMarkerConsts` values, including `MAX_RAID_TARGETS_USER`; it is a `Constants` table, not `Enum`. RED then GREEN (`2/2`) covers the source-backed registration boundary. See `/tmp/forever-raid-marker-ledger.json`. |
| Pixel round layout | `5a9dea71e` | Exposes existing round-layout Get/Set methods to Forever and PTR without renderer changes. Actual `PixelUtil.SetRoundLayoutToNearestPixelRecursively` proof is GREEN (`1/1`) across a Frame, child, Texture, and FontString: 10.3×20.3 rounds to 10×20 then restores. |
| Specialization class IDs | `9b86850b4` | Adds Forever `C_SpecializationInfo.GetAllClassIDs()` from the existing class catalogue, returning fresh numeric arrays. RED (`0/2`) reproduced `ClubFinder.lua:80` `ipairs(nil)`; GREEN (`2/2`) includes full vendor ClubFinder initialization. This is not a native-roster claim. |
| Combat-audio pulse enum | `8c3fc1025`, `0d8f30dc1` | Publishes source-backed Forever `CombatAudioAlertPulsePercentValues` (0–9) and metadata. GREEN (`4/4`) covers generated-documentation equality and the isolated vendor command block; full startup remains separate. |
| Batch-five events and enums | `1b29df37a` | Publishes two documented Forever events plus documented aura/curio enum metadata without enabling a retail epoch. RED (`0/3`) then GREEN (`3/3`) covers registration/unregistration, the actual `Deprecated_12_1_0.lua` consumer, and curio key usage. |
| Tracked initiative membership | `61becf6e7` | Reuses only the documented tracked-membership methods for Forever, retaining the existing model rather than enabling a retail epoch. RED (`0/4`) then GREEN (`4/4`) covers actual objective-tracker initialization, empty layout, relevant events, and untracking. It does not model task records, populated layout, emitted events, persistence, or native conformance; this post-batch-five focused proof does not reduce the recorded startup count. |
| Remaining documented events | `af05c04e8` | Adds only the three rejected documented Forever events: `DISCORD_GUILD_LOBBY_UPDATE`, `DISCORD_GUILD_SETTINGS_UPDATE`, and `SOCIAL_UI_FRIENDS_LIST_SYSTEM_STATUS_UPDATED`. `NEW_MATCHMAKING_PARTY_INVITE` was already accepted and remains unchanged. RED (`0/1`) then GREEN (`4/4`) covers the registration boundary; no event producer or native delivery is claimed. |
| Dock overflow texture animations | `13ca52c1c`, `016aec606`, `32c666fd4` | XML button-slot textures now receive their authored animation groups. The actual DockManager overflow `HighlightTexture` went RED (`0/1`) with missing `FlashAnim`, then GREEN (`4/4`) for one owned group, identity, `StopFlash`, and unchanged `FCFDockOverflowButton_UpdatePulseState` behavior. See [[forever-chat-overflow-slot-animations]]. |
| Raid-marker search | `0e9ed35d9`, `09e308635`, `7119e98d8` | Models documented `GetNextAvailableRaidTargetMarkerIndex()` from existing raid-target assignments. RED (`0/2`) reproduced the absent global; GREEN (`5/5`) covers occupied-marker skipping, direction, wrap, exhaustion sentinel `0`, dead/nonfriendly candidates, and an actual Shared consumer click that assigns marker 2 after occupied 1 then suggests 3. The corrected overlay assertion runs after the vendor consumer's state update; no renderer change or native-conformance claim. See `/tmp/forever-marker-search-ledger.json`. |
| Atlas CSV schema | `9aeff00b1` | The Forever atlas generator now requires all six named source columns and rejects missing headers, truncated rows, and invalid numeric fields with contextual errors. It removes positional and `set`/`canvas` default fallbacks. RED (`0/3`) then GREEN (`4/4`) covers reordered valid columns, malformed inputs, and generated geometry. See `/tmp/atlas-schema-ledger.json`. |
| Legacy and level constants | `333570f3a` | Publishes nine source-backed Forever legacy/level constants needed by the Camelot no-config talent-unlock consumer without changing trait models. The focused finite-constants suite is GREEN (`5/5`) after the two missing constants were reproduced. See `/tmp/forever-legacy-constants-ledger.json`. |
| Warning cleanup | `d961111bd` | Removes the unused table-helper import reported by the initial default-profile checkpoint; it changes no runtime behavior. |

## Graveyard-port predicate

`a13a97aba` adds Forever-only `CanPortGraveyard()` backed by independent player availability state, defaulting to false as simulator policy. Actual `GhostFrame.lua` OnLoad tests pass **2/2** after reproducing the missing global: unavailable hides the frame; available preserves its existing visibility; state changes and environment isolation work. Other-profile exclusion is tested in code but awaits final verification. No automatic death/ghost rules or port action are inferred. See [contract](specs/forever-graveyard-port.md) and `/tmp/forever-graveyard-proof.json`.

## Proof

- The initial independent `require` verifier recorded formatting, default and Forever checks, ten module-loader tests, six Forever sandbox tests, and six default sandbox tests passing. See [addon module imports](specs/addon-module-imports.md).
- At `65139f909`, the rebuilt Forever sync completed `4,398/4,398` extraction. This proves manifest mapping and cache synchronization; one file used existing CDN recovery.
- The first `--no-addons --no-saved-vars lua-errors` capture after sync exited `1`, with 422 distinct records and 514 occurrences. Its artifacts remain historical evidence under `/tmp/wowforever-startup.{stdout,stderr}`; the checked-in [Forever error baseline](baselines/wowforever-lua-errors.json) now reflects batch fifteen's clean `[]` result.
- Pinned batch-two startup at `725abfb8b` rebuilt warning-free, then exited `1` with **141 records / 161 occurrences**. Artifacts: `/tmp/forever-ui-batch2-build.json`, `/tmp/forever-ui-batch2-startup.stdout`, and `/tmp/forever-ui-batch2-startup.stderr`. The isolated follow-up diagnostic identified the `Constants.lua` table-key failure as `LFG_CATEGORY_LAIR` at line 482, but no Forever source assignment has been found, so that early attempted fix was reverted; exact-build evidence later supported `3c800b221`. Batch three recorded **137 / 155**. Batch four recorded **81 / 97**, but its revision/hash are not independently linked to its build command, features, or working-tree state; it is historical evidence only, not clean-revision proof.
- Batch five built `0d8f30dc1` with only the untracked ICE artifact present, recorded its command/status/revision/hash in `/tmp/forever-ui-batch5-build.json`, and then exited `1` with **40 records / 56 occurrences**. Artifacts: `/tmp/forever-ui-batch5-startup.stdout`, `/tmp/forever-ui-batch5-startup.stderr`, and `/tmp/wowforever-ui-proof-ledger.json`. This precedes `1b29df37a` and `f52b57a15`; it remains failing startup evidence, not acceptance.
- Immutable batch six built `32c666fd4` with only the untracked ICE artifact, copied the binary, and recorded its SHA-256 in `/tmp/forever-ui-batch6-build.json`. It then exited `1` with **20 records / 36 occurrences** (`/tmp/forever-ui-batch6-startup.{stdout,stderr}`), after the overflow and event commits. This is a pinned improvement, still failing startup—not acceptance or native conformance.
- Immutable batch seven built `10826d74c` with only the untracked ICE artifact, copied the binary, and recorded its SHA-256 in `/tmp/forever-ui-batch7-build.json`. It again exited `1` with **20 records / 36 occurrences** (`/tmp/forever-ui-batch7-startup.{stdout,stderr}`). Errors advanced from missing `overrideMap` initialization to a nil map entry at `GamepadOverrideBarMixin.lua:122`; `TargetActionBars/Shared.lua:27` still called a nil function. This is failing-startup evidence, not acceptance.
- Immutable batch eight built `7119e98d8` with only the untracked ICE artifact and exited `1` with **18 records / 34 occurrences**. Its copied binary, SHA-256, command/status, and output are recorded in `/tmp/forever-ui-batch8-build.json` and `/tmp/wowforever-ui-proof-ledger.json`. It includes the marker-search work but remains failing startup.
- Immutable batch nine built `333570f3a` with only the untracked ICE artifact and exited `1` with **14 records / 22 occurrences**. Its copied binary, SHA-256, command/status, and output are recorded in `/tmp/forever-ui-batch9-build.json` and `/tmp/wowforever-ui-proof-ledger.json`.
- Immutable batch ten follows `3c800b221` and `36f9ee9e8`/`22064dca1`, records source/build identity in `/tmp/forever-ui-batch10-build.json`, and exits `1` with **10 records / 16 occurrences**. That run reported missing `Enum.GamepadStanceBarOverride` (subsequently implemented in `cbccbf6ad`) and `pairs(nil)`. Runtime source inspection identified the latter as `BuffFrame`'s generated event wrapper; direct replay reached `BuffFrame.lua:752`, where `C_Item.GetWeaponEnchantInfo` returned nil for both weapon slots. `bc6fbbe3e`/`e0e753442` now model that producer; batch ten remains historical failing-startup evidence, not acceptance or native conformance.
- Immutable batch eleven built `e0e753442` with only the untracked ICE artifact, copied the binary, and recorded its SHA-256, command, and working-tree status in `/tmp/forever-ui-batch11-build.json`. It exited `1` with **8 records / 8 occurrences** (`/tmp/forever-ui-batch11-startup.{stdout,stderr}`).
- Immutable batch twelve built `ad2785975` with only the untracked ICE artifact, copied the binary, and recorded its SHA-256, command, and working-tree status in `/tmp/forever-ui-batch12-build.json`. It also exited `1` with **8 records / 8 occurrences**. That historical run reached a nil Gamepad pet-slot base and its action-bar cascades; `2d2877b25`/`91a73d59d` subsequently resolve it through a documented simulator policy. The user has no beta access; no further beta capture is required.
- Immutable batch thirteen built the committed pet-storage/default-layout source with only the untracked ICE artifact, copied the binary, and recorded command, status, revision, and SHA-256 in `/tmp/forever-ui-batch13-build.json`. It exits `1` with **4 records / 4 occurrences**. The former pet initialization cascades are gone; remaining errors are `MainActionBarFrame.lua:223` nil unit-predicate calls. Artifacts: `/tmp/forever-ui-batch13-startup.{stdout,stderr}` and `/tmp/wowforever-ui-proof-ledger.json`. This is failing-startup improvement evidence, not acceptance.
- Immutable batch fourteen records **4 records / 4 occurrences** in `/tmp/forever-ui-batch14-build.json`. The unit-predicate root advanced to missing `SetPreferredGamepadInteractTarget` at `MainActionBarFrame.lua:255`; `7e449f911` subsequently models that producer.
- **Immutable batch fifteen** built `7e449f911` with only the untracked ICE artifact, copied the binary, and recorded command, status, revision, and SHA-256 in `/tmp/forever-ui-batch15-build.json`. `--no-addons --no-saved-vars lua-errors` exited **0** with stdout `[]`: **0 records / 0 occurrences**. Stderr contains startup timing plus headless `Sound: no audio device available`, but no Lua/UI warning or error candidate. `/tmp/forever-final-interactions.lua` then exited 0 and asserted both Gamepad page units' possess/stance mappings and pet IDs, default layout 0, all three weapon-enchant snapshots, BuffFrame update, interact-icon update, MainActionBar OnShow, and chat-overflow pulse; its output ends `FOREVER_ACCEPT_INTERACTIONS_OK` followed by `[]`. This is clean Forever UI startup and scoped affected-interaction proof; it is not native/hardware conformance.
- Independent checkpoint `/tmp/verify-forever-checkpoint/ledger.json` records `cargo fmt --check`, Forever and default `cargo check`, Forever integration **54/54**, addon-module `require` **12/12**, and Forever/default sandbox **6/6** each. The default check's one unused-import warning was removed in `d961111bd`. Delta checkpoint `/tmp/verify-forever-delta/ledger.json` records warning-free default and Forever checks at `7119e98d8`, reuses unchanged broader proofs, and independently accepts marker **5/5** and atlas **4/4** development evidence. Constants checkpoint `/tmp/verify-forever-constants/ledger.json` records `cargo fmt --check` plus warning-free default and Forever `cargo check` at `333570f3a`; it accepts the nine exact-documentation constants suite **5/5** and reuses only unchanged static profile-isolation evidence. The sourced-values checkpoint `/tmp/verify-forever-sourced/ledger.json` passed formatting/default/Forever checks but rejected the two-slot enchant contract. Correction checkpoint `/tmp/verify-forever-ranged/ledger.json` independently accepts `ad2785975`: all three exact-build slots, unchanged eight-value legacy result, warning-free formatting/default/Forever checks, fresh default legacy regressions **2/2**, and inspected Forever consumer proof **4/4**. None is clean-startup acceptance.
- The earlier 334 / 496 startup capture predates later causal fixes. A separate 222 / 290 diagnostic ran against a concurrently rebuilt, scope-unknown binary and remains non-acceptance evidence. New finite-constants and global-string focused tests are warning-free GREEN (`2/2` each); full startup has not yet been rerun after `f954fc665`/`db875f554`.
- `5820b54e6` annotation routing proof is GREEN (`1/1` loader, `6/6` profile). `725abfb8b` verifies ten event names and `25c6ac778` extends the finite list; `68989ffcf` has targeted inventory-slot proof. These focused results do not establish a fresh full-startup count; the prior 334 / 496 startup capture reproduced their rejection, and only `INPUT_DEVICE_INTERFACE_TRANSITION` has current actual-InputUtil consumer proof (`2/2`). The remaining nine event paths await final verification. `6f9877555`/`df74659ea` name-policy proof is GREEN (`2/2`) through actual Camelot `NameUtil`; its concurrent roleset warning means it is not warning-free final evidence. `7da2b6028`/`343c4624b`/`f4800730c` texture-metatable proof went RED then GREEN (`1/1`) through actual `UnitFrameUtil.lua`; it has the same concurrent roleset-warning caveat. `5cd134dcd` atlas proof is GREEN (`1/1` real Lua cycle query and `1/1` generator fixture). `a0e6df424` input-style proof is GREEN (`2/2`), `916acf5e8` social-enum proof covers three documentation tables plus Communities sorting, `5955650e8` Edit Mode fixtures pass, `413fcd1ce` cooldown-category fixtures pass, `6f9877555` name-policy fixture passes, and `c20f26dad` Aura XML fixtures pass. `0a354c0e0` math and `b6a6a7dad` table development proof each went RED (`0/1`) then GREEN (`1/1`) against real Forever consumers, with no warnings. `799389a6e` timed-signal proof went RED (`0/6`) then GREEN (`6/6`) including the real Forever `TimerUtil.lua` wrapper, with no warnings. `5e2558264`/`7f6297984` enum proof is GREEN (`3/3`) against API documentation plus AccountUtil/sound consumers. `0d7942e64` securecopy proof is GREEN (`2/2`) without warnings for graph structure and real AuraShared; consumer RED was not cleanly observable because of unrelated compile/prerequisite blockers. `5e2558264`/`b838d6917` gamepad-constants proof went RED (`0/2`) then GREEN (`2/2`) against real `InitActionButtons`; it does not cover hardware input. `72fc53d24`/`5570fe263`/`d5ad14601` gamepad-stick script binding and generic dispatch proof is GREEN (`2/2`) without warnings; it also does not cover hardware input. `4deca63f1` event proof was RED on an unregistered documented event, then GREEN (`2/2`); its concurrent-build warnings prevent treating it as final integration proof. `a0e6df424` input-style proof is GREEN (`2/2`) through actual `InputUtil.lua` initialization and transition callbacks. `916acf5e8` social-enum proof is GREEN (`2/2`) through API-documentation value checks and actual Communities sorting. The latest recorded full startup before these commits remains exit `1`, 334 records / 496 occurrences; neither focused result establishes its post-commit count. Final independent verification remains pending.

## Pending fixes

The startup blocker list is empty at batch fifteen. Remaining scope limits:

- `CooldownViewerSecure.lua` and loader-boundary table-security proof is GREEN (`4/4`) at `4f92e7cb0`/`60ae94004`; it is not a full secret-value VM or native-conformance claim.
- Do not use the 222 / 290 concurrent-binary diagnostic or batch-four result as clean-HEAD acceptance.
- Generic Gamepad stick dispatch does not establish host-gamepad input support. Pet storage base `1` is a documented simulator policy, not native numeric conformance.
- Modern (`0`) is the named initial EditMode default; dynamic native selection across input styles remains unverified.
- Independent final verification is running separately; this report does not replace it.

## Limitations

- No native WoW execution or native-conformance claim.
- A synchronized cache does not establish clean startup, panel interaction, rendering parity, gamepad input dispatch, or full UI compatibility.
- `GetBuildInfo()` date and trailing return fields retain existing temporary defaults.
- `require` preserves only the Warcraft Wiki contract covered by its [canonical spec](specs/addon-module-imports.md); unspecified native edge cases remain unmodeled.

## Canonical documents

- [Client profiles](specs/client-profiles.md) — supported profile contract and feature isolation.
- [Forever table security](specs/forever-table-security.md) — native registration order, profile boundary, Cooldown Viewer contract, and remaining loader-boundary gate.
- [Addon module imports](specs/addon-module-imports.md) — `require` behavior and exclusions.
- [Updating Blizzard UI](updating-blizzard-ui-to-a-new-patch.md) — manifest/listfile refresh and residual-miss workflow.
- [Profile baselines](baselines/README.md) — startup-baseline interpretation.
- [Client profile system](wiki/systems/client-profiles.md) — loader/cache architecture.
