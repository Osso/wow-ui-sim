# Native API contract probes

Manual `public-queries` observes ten no-argument queries twice each: GameRules, Delves UI with companion omitted, housing-market availability, encounter time, three instance-encounter flags, active outfit ID, exterior-door hover, and spell-diminish support. Manual `neighborhood-state` separately observes seven no-argument initiative state queries twice each, while `neighborhood-structures` bounds three initiative producers and original tracked task IDs; `sets-catalog` records bounded available-set entries and filter state; `custom-set-names` records bounded custom-set IDs and original names. These fixtures prove recorder mechanics only; native ruleset, housing, encounter, clock, outfit, hover, diminish, companion-policy, trait-tree, collection, neighborhood, and restricted-context behavior remain pending.

Scalar curve point, `UnitSexBase`, unit-name/realm, finite numeric-formatting, cast/channel identity, selected action-slot, scalar resource scale, plain callback lifecycle, and residual `StripHyperlinks` investigations share one manual [ApiContractProbe](../../addons/ApiContractProbe/README.md). The existing [AuraDispelCurveProbe](../../addons/AuraDispelCurveProbe/README.md) covers dispel-ID input. Runtime `3f7934e27` adds `names`; `41bcd89f0` adds numeric capture; both are included in `all`. These recorder experiments are prepared; none was installed or executed in a native client by this task. They do **not** cover all remaining probe needs.

## Evidence boundaries

`/apicontract sets-catalog <label>` is manual-only and excluded from `all`. It calls `C_TransmogSets.GetAvailableSets()` once and `IsUsingDefaultSetsFilters()` twice without arguments, preserving raw arity, nil positions, and opaque errors. Only first-return list entries 1–8 expose guarded `setID`, `name`, `collected`, `favorite`, `validForCharacter`, and declared `grantAsPrecedingVariant` fields. It caps calls at three per snapshot under the shared ten-snapshot and 128-byte-label bounds. No downstream ID queries, setters, reset, selection, ordering, completeness, defaults, field semantics, or native behavior is established. Fourteen cumulative fixtures, including three new cases, prove recorder mechanics only.

`/apicontract custom-set-names <label>` is manual-only and excluded from `all`. It calls `GetNumMaxCustomSets()` twice and `GetCustomSets()` once, preserving raw arity, nil positions, and opaque errors. Only first-return ID-table entries 1–4 are inspected with guarded access. Each accessible finite original ID permits one `GetCustomSetInfo(id)` call; only its accessible original string first return permits one `IsValidCustomSetName(name)` call. Names are forwarded untruncated, while recorded scalar strings remain capped at 256 bytes. It makes at most eleven API calls per snapshot under the shared ten-snapshot and 128-byte-label bounds. It does not mutate custom sets or forward list/hyperlink tables; name validity, identity, defaults, and native behavior remain unverified. Ten fixtures prove recorder mechanics only.

`/apicontract outfit-slots <label>` is manual-only and excluded from `all`. It independently calls no-argument `GetAllSlotLocationInfo` and `GetSlotGroupInfo`, preserving raw tuples. It inspects only the first two location lists through entries 1–8 and guarded `slot`, `type`, `collectionType`, `slotName`, and `isSecondary` fields. Only up to sixteen accessible finite original location slots feed one each of `GetEquippedSlotOptionFromTransmogSlot(slot)` and `GetUnassignedAtlasForSlot(slot)`. The first group list is likewise capped at eight entries; its `position`, `appearanceSlotInfo`, and `illusionSlotInfo` receive bounded guarded inspection, but never feed queries. No recursion, mutation, 3D behavior, ordering/completeness, mapping, atlas, or native claim follows. Eleven fixtures prove recorder mechanics only.

`/apicontract outfit-catalog <label>` is manual-only and excluded from `all`. It calls `C_TransmogOutfitInfo.GetOutfitsInfo()` once, preserving its raw tuple, then inspects only entries 1–8 of an accessible first result table. Each entry exposes only guarded `outfitID`, `name`, `icon`, `isEventOutfit`, `isDisabled`, `playerFacingOutfitIndex`, and `situationCategories`; each category sublist is capped at scalar positions 1–8 without iteration. An accessible finite original outfit ID permits one independent `GetOutfitInfo(id)` call whose first result receives the same non-recursive inspection. It performs no mutation, outfit selection, name query, recursion, ordering/completeness/identity claim, or native conclusion. Eleven fixtures prove recorder mechanics only.

`/apicontract outfit-state <label>` is manual-only and excluded from `all`. It calls `GetCurrentlyViewedOutfitID`, `GetMaxNumberOfUsableOutfits`, `GetNextOutfitCost`, `GetPendingTransmogCost`, `HasPendingOutfitSituations`, `IsEquippedGearOutfitDisplayed`, and `IsEquippedGearOutfitLocked` twice each with no arguments. It preserves raw arity, including pending-cost's two-return or zero-return cases, without mutations, purchases, selection, price interpretation, default/stability claims, or native conclusions. Ten fixtures prove recorder mechanics only.

`/apicontract spell-diminish-categories <label>` is manual-only and excluded from `all`. It resolves only the fixed published `SpellDiminishRuleset` names `None`, `PvE`, and `PvP`, and the fixed `SpellDiminishCategory` names `Root`, `Taunt`, `Stun`, `AoEKnockback`, `Incapacitate`, `Disorient`, `Silence`, and `Disarm`. It independently calls `GetAllSpellDiminishCategories` for each ruleset plus `GetSpellDiminishCategoryInfo` for each category. Only entries 1–8 of an accessible first returned list and the first accessible info object expose guarded `category`, `name`, and `icon` fields. It does not call secret `ShouldTrackSpellDiminishCategory`, observe tracker events, mutate state, infer category semantics, or establish native behavior. Eleven fixtures prove recorder mechanics only.

`/apicontract weekly-progress <label>` is manual-only and excluded from `all`. It resolves only five fixed published `WeeklyRewardChestThresholdType` names (`Raid`, `Activities`, `World`, `RankedPvP`, `Concession`) and independently calls `C_WeeklyRewards.GetSortedProgressForActivity` with `false` then `true`. Only the first accessible returned table has entries 1–8 inspected, and each entry exposes only guarded declared `activityTierID`, `difficulty`, and `numPoints` fields. It neither iterates enums nor falls back to numeric values, traverses or mutates result tables, or concludes sorting, combine behavior, completeness, or native behavior. Eleven fixtures prove bounded recorder mechanics only.

`/apicontract housing-preview-modes <label>` is manual-only and excluded from `all`. It resolves only six fixed published `Enum.HouseEditorMode` names (`BasicDecor`, `ExpertDecor`, `Customize`, `Cleanup`, `Layout`, `ExteriorCustomization`) and calls `C_HousingDecor.IsModeDisabledForPreviewState` twice per accessible finite value. It neither iterates enums nor falls back to numeric values. Mode changes, preview-state mutation, LoD loads, repeated-read/default claims, and native conclusions are excluded. Eight fixtures prove bounded recorder mechanics only.

`/apicontract unit-target-display <label>` is manual-only and excluded from `all`. It independently calls only `UnitShouldDisplaySpellTargetName(unit)` twice for each fixed token: `player`, `target`, `focus`, `party1`, `nonexistent`, `invalid-unit-token`, and the empty string. Token access is guarded before and after function guards; arity, nils, accessible scalars, and opaque errors remain raw observations. Secret-return `UnitSpellTargetClass` and `UnitSpellTargetName`, cast queries, mutations, and default or repeated-read claims are excluded. Eight fixtures prove bounded recorder mechanics only; native target-display behavior remains unverified.

`/apicontract unit-auras-current <label>` is manual-only and excluded from `all`. It makes three independent `C_UnitAuras.GetUnitAuras` calls: `("player")` is intentionally the missing-required-filter negative case, while `("player", "HELPFUL", 8)` and `("player", "HARMFUL", 8)` omit sort arguments. Only an accessible first returned table has entries 1–8 inspected, and each table/userdata entry exposes only guarded `auraInstanceID`, `spellId`, and `applications` observations. Those fixed fields come from pinned `AuraUtil.lua` consumers, not an `AuraData` declaration. It makes no ordering, completeness, identity, sorting, default, security, or native-contract claim. Nine fixtures prove bounded recorder mechanics only.

`/apicontract aura-time <label>` is manual-only and excluded from `all`. It reuses one first-page `GetAuraSlots("player", "HELPFUL", 8)` producer and forwards at most eight guarded original aura instance IDs from `GetAuraDataBySlot("player", slot)`. Each eligible ID receives independent two-argument calls to `DoesAuraHaveExpirationTime`, `GetAuraBaseDuration`, `GetRefreshExtendedDuration`, and `GetAuraDuration`; optional spell IDs are omitted. For each accessible `GetAuraDuration` table/userdata return at positions 1–16, it runs the current-only ten-method whitelist without retention. Receiver access is checked before inspection and after method lookup/function guards immediately before invocation. No field traversal, evaluate/set/copy method, continuation traversal, mutation, or retention occurs. Eleven cumulative aura-time fixtures (three new), eight display-count fixtures, nine spell-duration fixtures, and two main-harness regressions prove recorder mechanics only. Native expiration, duration, refresh, lifecycle, defaults, coercion, restricted-context behavior, and all native results remain unverified.

`/apicontract aura-display-count <label>` is manual-only and excluded from `all`. It calls `C_UnitAuras.GetAuraSlots("player", "HELPFUL", 8)` once, preserves its continuation and slot tuple, and does not traverse continuation. At most eight accessible finite original slot values produce guarded `auraInstanceID` values through `GetAuraDataBySlot("player", slot)`. Each ID receives five independent `GetAuraApplicationDisplayCount` calls: omitted optional arguments, `(1)`, `(2)`, `(2, 5)`, and `(1, 1)`. Namespace/function and producer values are guarded and rechecked before forwarding. Eight separate fixtures prove bounds, arity, errors and access behavior only; no native ordering, formatting, coercion, defaults, enumeration completeness or native behavior follows.

`/apicontract spell-duration <slot> <label>` is manual-only and excluded from `all`. It uses the existing integer-slot parser and calls `GetActionInfo(slot)` with exactly one argument. Only an accessible first result exactly equal to `"spell"` and accessible finite numeric second result authorize the original spell ID for `C_Spell.GetSpellChargeDuration` and `C_Spell.GetSpellLossOfControlCooldownDuration`.

Protected namespace/function lookup and both producer values are guarded and rechecked immediately before each call. Missing, restricted or failed producers are unavailable inputs, not zero; one unavailable producer does not suppress the other. Current returned objects alone receive the existing ten read-only duration methods. No objects are retained between snapshots, and this mode neither reads nor changes cast-duration retention state or its counter.

Raw arity, nil positions and opaque errors remain preserved under existing sixteen-position, 256-byte string, 128-byte label and ten-snapshot bounds. At most two producers and 320 method calls run per snapshot. Nine separate actual TOC/slash fixtures prove routing, isolation and bounds only. Charge/recharge and loss-of-control transitions, spellbook behavior, native timing and restricted-context behavior remain pending.

`/apicontract spellbook-metadata <slot> <label>` is manual-only and excluded from `all`. It uses the existing integer-slot `GetActionInfo(slot)` producer, then only an accessible original `"spell"` kind and finite numeric ID authorize three independent `C_SpellBook` calls: `FindBaseSpellByID`, `FindFlyoutSlotBySpellID`, and `FindSpellOverrideByID`. Producer values are guarded before lookup and rechecked after lookup/function guards; each raw tuple, nil position, opaque error, and peer failure remains independent. Eight fixtures prove bounded recorder mechanics only. At runtime `63bb28063`, it neither discovered spellbook slots nor queried item durations. Native spellbook classifications, transitions, and security remain unverified.

`/apicontract spellbook-duration <slot> <label>` is manual-only and excluded from `all`. It forwards an accessible original selected-action spell ID to `FindSpellBookSlotForSpell(id, false, true, true, true)`, preserving its raw result while forwarding only its original accessible finite numeric slot/bank pair. Charge, cooldown with `false`, and loss-of-control duration queries are independent; accessible current results receive the existing ten-method read-only whitelist without retention. Ten fixtures plus eight spellbook-metadata regressions prove recorder mechanics only. Native slot/bank semantics, durations, transitions, and restricted behavior remain unverified.

`/apicontract spell-metadata <slot> <label>` is manual-only and excluded from `all`. `GetActionInfo(slot)` runs with exactly one argument; only an accessible first result exactly equal to `"spell"` plus an accessible finite numeric second result authorizes the original spell ID for seven independent `C_Spell` metadata/predicate queries and `C_UnitAuras.AuraIsBigDefensive`. Each query preserves arity and nil positions and remains independent after peer lookup or call errors. No spell ID is converted, guessed, classified, or propagated from a nil producer result. `GetVisibilityInfo` additionally uses the three fixed published `SpellAuraVisibilityType` names (`RaidInCombat`, `RaidOutOfCombat`, `EnemyTarget`), with guarded finite values and no numeric fallback. Nineteen cumulative fixtures, including five added aura-defensive cases, prove bounded routing and guards only. Native spell/aura fixtures, rank/override transitions, classifications, and security behavior remain pending.

`/apicontract public-queries <label>` is manual-only and excluded from `all`. It calls ten no-argument APIs twice each: the original GameRules and omitted-companion Delves UI queries; housing-market availability; encounter time; three `C_InstanceEncounter` flags; `C_TransmogOutfitInfo.GetActiveOutfitID`; `C_HousingCustomizeMode.IsHouseExteriorDoorHovered`; and `C_SpellDiminish.IsSystemSupported`. Protected namespace lookup and accessibility guards precede inspection; missing APIs, lookup failures and opaque errors do not suppress peers. Twenty calls per snapshot remain under the existing bounds. Seventeen cumulative fixtures, including five new cases, prove bounded recorder mechanics only. Native housing, encounter, clock, outfit, hover, diminish, ruleset, companion-policy, trait-tree, default, state-transition, and restricted-context behavior remain pending.

`/apicontract neighborhood-state <label>` is manual-only and excluded from `all`. It independently calls `C_NeighborhoodInitiative.GetActiveNeighborhood`, `GetRequiredLevel`, `IsInitiativeEnabled`, `IsPlayerInNeighborhoodGroup`, `IsViewingActiveNeighborhood`, `PlayerHasInitiativeAccess`, and `PlayerMeetsRequiredLevel` twice with zero arguments. It preserves raw tuples, nil positions, opaque errors, and guarded scalar observations under fourteen calls per snapshot, the shared ten-snapshot cap, sixteen result positions, 256-byte strings, and 128-byte labels. It makes no requests, active/viewing changes, claims, contributions, task queries, structured initiative/activity-log queries, or native group/default/stability conclusions. Ten fixtures prove recorder mechanics only; native fixtures, transitions, and restricted-context behavior remain unverified.

`/apicontract neighborhood-structures <label>` is manual-only and excluded from `all`. It captures `GetNeighborhoodInitiativeInfo`, `GetInitiativeActivityLogInfo`, and `GetTrackedInitiativeTasks` once each, inspecting only first returned objects and fixed scalar fields. Initiative `tasks` and `milestones`, plus task `requirementsList` and `criteriaList`, remain opaque. Activity is capped at entries 1–8; tracked original accessible finite IDs are capped at 1–4 and independently feed `GetInitiativeTaskInfo(id)` and `GetInitiativeTaskChatLink(id)`. It makes at most eleven calls per snapshot under shared tuple/string/label and ten-snapshot bounds. No requests, setters, tracking mutations, nested traversal, ordering/completeness, or native behavior is established. Eleven fixtures prove recorder mechanics only.

`/apicontract item-binding <label>` is manual-only and excluded from `all`. For player equipment slots 1–19, it records `GetInventoryItemLink` arity and uses only the first actual producer return when it is an accessible non-secret string. Each eligible original link is passed once to `C_Item.IsItemBindToAccount`; truncated stored text, synthetic IDs/options, alternate producers and account mutations are excluded. Missing, nil, nonstring or failed producer results are `unavailable-input`, not false; restricted links are `restricted-input`. Eight separate fixtures prove routing, guards and bounds only. Native binding classifications, known/unknown fixtures, output and security semantics remain pending.

`/apicontract statusbar-fill <label>` is manual-only and excluded from `all`. It creates one unnamed StatusBar under `UIParent`, attempts `Hide`, and captures fresh `GetFillStyle()` only after Hide succeeds. It reads the four published `Enum.StatusBarFillStyle` names (`Standard`, `StandardNoRangeFill`, `Center`, `Reverse`) without numeric fallback; every accessible finite scalar produces one `SetFillStyle` and two independent subsequent getter observations. Arity and nil positions remain raw; inaccessible values and errors remain opaque. Constructor or Hide failure stops fill operations and retains no object reference. A failed Hide leaves visibility unconfirmed; the recorder makes no unsupported cleanup or disposal claim. The seven separate local fixtures prove routing and bounds only. Native defaults, invalid-input/coercion, security, rendering and validation behavior remain pending.

`/apicontract heal-calculator <label>` is manual-only and excluded from `all`. Every snapshot attempts two fresh no-argument `CreateUnitHealPredictionCalculator()` calls. Each accessible calculator receives `GetHealAbsorbMode`, `GetHealAbsorbClampMode`, `GetDamageAbsorbClampMode`, `GetHealAbsorbs`, and `GetDamageAbsorbs` twice. Repeated observations are retained independently, without equality or stability conclusions. Constructor and getter tuples preserve arity and nil positions through the scalar/opaque observer; inaccessible values and errors remain opaque. A separate seven-fixture suite validates recorder mechanics, not client behavior. Pinned retail documentation establishes the constructor and getter signatures only. No unit variants, setters, reset/default transitions, populated state, native execution, or security conclusion is included; all remain pending.

`/apicontract raid-markers <label>` is manual-only and excluded from `all`. It independently calls `CanBeRaidTarget(unit)` twice for nine fixed tokens, `IsRaidMarkerActive(index)` twice for indices 1–8, and no-argument `IsRaidMarkerSystemEnabled()` twice: 36 calls total. Missing APIs never suppress another query, and repeats stay raw rather than becoming stability conclusions. Accessibility checks precede inspection; arity, nil positions and bounded scalar output remain observable while objects and errors stay opaque. The separate six-fixture suite proves only routing, bounds and recorder mechanics. `GetRaidTargetIndex` is excluded for its secret return; no set, clear, place or remove call runs, and no permission, security, native result or populated-world-marker claim is made.

Scalar point declarations specify vectors but do not establish indexing, ordering or returned-object identity. The recorder observes empty/populated curves, explicit index queries, accessible fields/GetXY, repeated equality and an isolated mutation attempt. Cached `Blizzard_SharedXML/Vector2D.lua` provides vector fields but does not prove curve return behavior.

`/apicontract curve-state <label>` is manual-only and excluded from `all`; it leaves `curves` unchanged. Runtime `411edf39e` captures empty and populated unsorted duplicate-point scalar curves, accessible type/count/points/secret-flag, evaluations at seven fixed inputs, and results before/after reset. It separately records a copied curve before and after original mutation and clearing, without classifying defaults, duplicate handling, interpolation or copy isolation.

Runtime `9b7fdb43e` adds independent manual `/apicontract curve-edit <label>`, also excluded from `all`. Fresh scalar curves receive `RemovePoint` indices `-1, 0, 1, 2, 3, 4`; separate fresh curves receive `SetPoints` with empty and unsorted duplicate inputs. Replacement vectors come only from accessible `CreateVector2D`, and each case retains definitions, bounded point/count state, `Evaluate(15)`, mutation arity and opaque errors. The cumulative harness has 41 fixtures; that is not 41 new native cases. Native curve index base, ordering/duplicates, returned-copy behavior, `SetPoints` coercion and secret semantics remain capture work.

Runtime `ffd3b08ca` adds manual `/apicontract color-curves <label>`, also excluded from `all`. It constructs one owned curve with four color points, then a copy, records raw state, point lookups and packed/unpacked evaluation, and observes reset/copy mutation without classifying defaults or isolation. Returned tables expose only guarded raw RGBA fields; userdata stays opaque and no returned methods or metamethods run. A separate color harness has four fixtures; the existing harness retains 41. Neither suite is native execution. Native userdata field representation, identity, coercion, security, removal/replacement and alternate interpolation behavior remain unexecuted.

`UnitSexBase` returns `Enum.UnitSex`; legacy `UnitSex` numbering cannot be copied blindly. The recorder independently stores `UnitExists`, raw `UnitSex`, and raw `UnitSexBase` observations for player, target, focus, pet, party1, party2, nonexistent, invalid-unit-token, and the empty token, plus named enum values. An absent, restricted, or failed existence result does not suppress either sex query; each unit has separate `exists`, `legacy`, and `base` records rather than an aggregate status. It does not infer what “Base” means. Native before/during/after-disguise captures still require independently established stable unit identity.

Name/realm capture invokes `UnitName` and `UnitNameUnmodified` without existence gating for fixed player, party, target, unknown and invalid-token inputs. It preserves exact arity and positional nils, so nil and empty realms remain distinct from explicit realm strings. Scenario labels identify independently assembled same-realm and cross-realm party slots; returned names never classify the session. Native same-realm and cross-realm sessions remain pending. Actual populated-slot, recharge, consumable transition, and duration-semantics captures remain pending.

`/apicontract numbers <label>` records raw `GetLocale` provenance plus exact arity, positional nils, and bounded accessible result bytes from both `C_StringUtil.FloorToNearestString` and `C_StringUtil.RoundToNearestString`. Its ordered 25 finite inputs cover signed ties and close neighbors, integers, fractions, and large finite magnitudes. It records literal observations only; helper names do not establish rounding or grouping semantics. Nonfinite inputs, coercion, security behavior, values outside this corpus, and actual build/locale captures remain unknown.

`/apicontract casts <label>` records `UnitCastingInfo` and `UnitChannelInfo` for fixed player, target, focus, party, unknown, invalid and empty-unit controls. It preserves exact arity and nil positions through sixteen scalar-only return slots; values beyond that bound are marked truncated. Returned objects retain only status/kind: the recorder never invokes methods, indexing, equality or string conversion. `all` includes casts. The matching client and manual sequences for ordinary casts, cancellation, replacement/consecutive casts, non-player channels, and empowered/non-empowered channels remain pending; spell IDs and fixture actions are intentionally not invented.

`/apicontract cast-durations <label>` is manual-only and excluded from `all`. It captures `UnitCastingDuration`, `UnitChannelDuration`, and `UnitEmpoweredChannelDuration` with empowered hold omitted, false, and true. Accessible duration objects receive only ten guarded read methods; no mutation, equality, or object-identity operation runs. A later capture may re-query at most 28 prior object references to observe tracking versus frozen state without asserting either behavior. The main harness has 43 cumulative fixtures; its separate color harness has four. Neither suite is native execution. Matching-client active, completed, interrupted and absent cast/channel/empower states plus clock boundaries remain pending.

`/apicontract actions <slot> <label>` records one manually selected action slot; `all` deliberately excludes it. It captures `GetActionInfo`, display count with default and three explicit thresholds, and `GetActionCharges` arity plus raw `currentCharges`, `maxCharges`, `cooldownStartTime`, `cooldownDuration`, and `chargeModRate` fields. `GetActionChargeDuration` retains only accessible kind/status and arity, never values or methods. It does not call `GetActionCooldown`, infer slot roles, or execute actions. Actual populated-slot, recharge, consumable, before/use/restoration transition, and duration-semantics captures remain pending.

`/apicontract resources <label>` is an explicit residual best-effort recorder; `all` excludes it. It records player, target, focus, pet and nonexistent-unit raw health/max, power/max/type, omitted/nil curve queries, and explicit `unmodified=false`/`true` power queries. A six-point scalar linear curve spans `0`, `.5`, `1`, `25`, `50` and `100`; observations record its inputs and raw results rather than classifying native input scale. It neither invents power IDs nor changes resources. Color curves, prediction, secondary power, actual partial-resource fixtures, native event ordering and security behavior remain pending. Its 29 local fixtures prove recorder mechanics only; this remains outside the snapshot and earns no native credit.

`/apicontract hyperlinks <label>` is an explicit residual best-effort recorder; `all` excludes it. It submits 16 literal inputs across nine optional-flag variants (144 calls), recording input bytes, flags, argument count and bounded raw results. The corpus includes balanced and incomplete markup, Unicode, literal `|n`, newline, escaped pipes and an orphan suffix. It makes no expected-output claim. Native execution remains pending; malformed/nested semantics, coercion, arbitrary bytes, string-view lifetime and security behavior remain unknown. This residual recorder is neither a new audit credit nor completion of an active snapshot plan.

`/apicontract cast-durations <label>` is manual-only and excluded from `all`. It queries `UnitCastingDuration`, `UnitChannelDuration`, and `UnitEmpoweredChannelDuration` with omitted, false, and true empowered-hold inputs across fixed unit tokens. Accessible duration objects receive only ten guarded read-only method calls; results retain scalar/opaque arity. At most 28 prior object references are re-queried on the next capture to observe, without classifying, frozen versus tracking behavior. The 43 main-harness fixtures are recorder proof only; the color harness remains a separate four-fixture suite. Matching-client active/completed/interrupted/absent cast, channel, empower, and clock-boundary captures remain pending. No casts, timers, object mutation, identity conclusion, or native security claim is made.

`/apicontract callbacks-start <label>` registers only this addon's plain Lua callbacks for global `UNIT_HEALTH` and player-filtered `UNIT_HEALTH`; `callbacks-stop` removes the retained identities. It records registration/removal results and up to 128 passive deliveries with exact arity and up to sixteen safe scalar positions. Partial registration and cleanup errors remain recorded rather than treated as stopped. It neither fires events nor examines wrapper containers. The 33 local fixtures prove recorder mechanics only. A native producer, post-removal delivery boundary, `Event.lua` FunctionContainers, duplicates, ordering, aliases, mutation/recursion and security behavior remain pending. This residual best-effort work does not complete the earlier simulator callback credits or a snapshot plan.

`/apicontract mapvalues <label>` is manual-only and excluded from `all`. It calls the actual global as `mapvalues(callback, ...)` across zero/one/multiple inputs, interior and trailing nils, and fixed single/multiple/nil/zero callback returns plus an opaque callback error. It retains callback invocation tuples and outer results with up to sixteen positions and a shared 32-invocation cap. The separate `mapvalues.lua` suite has eight fixtures; the main harness remains 44 and the color harness four—these are not 56 new native cases. Native packing, order, callback-failure propagation and security remain unobserved.

`/apicontract abbreviations <label>` is manual-only and excluded from `all`. It calls `AbbreviateLargeNumbers` and `AbbreviateNumbers` independently for the shared 25 finite inputs, with options omitted, and records one `GetLocale` observation. Scalar tuples retain arity, nil positions and bounded accessible bytes; errors and inaccessible results remain opaque. The separate abbreviation suite has six fixtures; the main harness remains 44. This does not capture `NumberAbbrevOptions`, configuration, table producers, locale mutation, or native abbreviation semantics.

Dispel curve numeric IDs remain pending the separate prepared capture. See [[aura-dispel-curve-probe]].

## Sex-control local proof

Runtime `9d88dc424` removes existence-gating from sex queries; `1bedd3ce9` updates the restricted-existence fixture. The main harness has 44 cumulative fixtures; this is recorder proof only, not a native sex-value, disguise, or identity contract.

## Local proof

Tests `e2adbb57d` initially failed because the new addon did not exist. The scoped recorder proof has since grown by focused commits; scalar-curve edit runtime `9b7fdb43e` brought the harness to 41 local fixtures, and color-curve runtime `ffd3b08ca` adds four scoped fixtures to that existing base. These counts are cumulative harness size, not per-slice native evidence. Callback fixtures cover start/stop, identity cleanup, bounded payloads, redaction and failure retention. Fixtures remain recorder proof only, not native contracts. Cast fixtures cover scalar tenth/eleventh positions, nil positions, empowerment values, repeated/consecutive snapshots, zero returns, opaque errors, restricted values, hostile objects, the sixteen-position bound, and `all` inclusion. Action fixtures cover selected-slot command parsing, five raw charge fields, display observations and opaque duration handling; they do not establish action semantics. Actual TOC/slash/SavedVariables wiring remains locally exercised.

The name recorder's prior 13 fixtures plus 12 supplemental checks remain valid within their earlier scoped commit. Numeric, cast, action, and hyperlink fixtures prove literal recording only, not native behavior. Fixture outputs are not native contracts. No audit credits or totals changed. Actual native captures still require matching-client same-/cross-realm party sessions, build/locale numeric sessions, and controlled cast/channel/empower fixture sequences before assigning compatibility evidence.

## Complete preparation inventory

[Preparation inventory](../../baselines/native-probe-preparation.json) retains all 282 snapshot plans: 275 active, covering 1,083 rows, and seven without active rows. Snapshot membership excludes best-effort residual gaps and exceptions; their separate reconciliation remains pending.

At `17948d0c9`, 112 plans have shared publication/event recording components, 19 have partially reviewed dedicated observations, 142 still need design/recorder review, and two are explicitly deferred security plans. Later recorder additions require inventory reconciliation before these counts are reused. No whole-plan completion is claimed from these counts. Existing older probes may supply more coverage but must be inspected before credit.

`AuditTargets.lua` supplies 199 publication/CVar and 88 event-registration targets. These capture parent/member/CVar observations and real event tuples, not missing producers, historical phases, transitions or semantic contracts. Runtime `e2af1e8e1` initially invoked payload methods during passive event inspection. Regression `5870cfa46` reproduces that error; `d959372a3` uses non-executing raw table inspection for passive payloads. Independent proof reuses 10/10 main fixtures and passes 21/21 supplemental checks, including zero payload `GetXY`/`__index` calls. Ledger: `/tmp/verify-all-probes-recorders-corrected-ledger.json`.

All-probe preparation remains open. No installation or native execution is authorized by recording-code preparation.

## Sources

- [Recorder spec](../../specs/api-contract-probe.md)
- [Manual capture protocol](../../addons/ApiContractProbe/README.md)
- [Dispel probe spec](../../specs/aura-dispel-curve-probe.md)

## See Also

- [[patch-12-0-0-api-audit]] — unresolved native contracts
- [[aura-dispel-curve-probe]] — companion experiment
